---
# Komodo images (GitHub Container Registry)
#
# Version EXPLICITE, pas le tag majeur flottant `2`. Komodo publie `:2`,
# `:2.0` et `:2.0.0` ; `:2` ne protege que d'un saut de major et laisse
# derriver minor et patch. Avec un tag flottant, la version reellement
# deployee est decidee par la DATE DU DERNIER RUN et non par le depot :
# dockhost est reste sur v2.1.2 (avril) pendant que hermes-30, provisionne en
# aout, recevait v2.3.2 — meme fichier, meme tag, deux binaires.
#
# ⚠️ Doit rester aligne avec `komodo_periphery_image_tag` du role
# `hermes/ansible/roles/komodo_periphery`. Un Core plus ancien que ses agents
# est le mauvais sens : l'UI affiche VERSION MISMATCH sur le Server concerne.
# renovate: datasource=docker depName=ghcr.io/moghtech/komodo-core
komodo_image_tag: "2.3.2"

# MongoDB — la base qui porte TOUT l'etat de Komodo.
#
# Le compose amont ecrit `image: mongo`, sans tag : c'est `latest`. Combine au
# `pull: always` de ce role, chaque replay du playbook re-tirait la derniere
# image publiee. MongoDB ne supporte pas de sauter une version majeure a la
# mise a niveau : un bond surprise laisse la base incapable de demarrer.
#
# Epingle sur la version qui tourne (v8.2.12), tag + digest.
# renovate: datasource=docker depName=mongo
komodo_mongo_image_tag: "8.2.12"
komodo_mongo_image_digest: "sha256:e0ce8c35124d4a9f9785532d1f268f39e9728ffa1cb38f46fa482436424c4bd3"

# Host paths
komodo_install_dir: "/opt/komodo"
komodo_backups_dir: "/etc/komodo/backups"
komodo_periphery_root: "/etc/komodo"

# Ports
komodo_core_port: 9120

# URL used by Komodo for OAuth/webhook callbacks + UI suggestion
# Override in group_vars/dockhost.yml with the actual reachable URL
komodo_host: "http://192.168.10.90:{{ komodo_core_port }}"

# Timezone
komodo_tz: "Europe/Paris"

# Initial admin
komodo_admin_username: "admin"

# First server declared in Komodo (name of the dockhost itself)
komodo_first_server_name: "dockhost"

# Database credentials (MongoDB)
komodo_db_username: "komodo"

# Polling intervals (see compose.env reference)
komodo_monitoring_interval: "15-sec"
komodo_resource_poll_interval: "1-hr"
komodo_jwt_ttl: "1-day"

# Feature toggles
komodo_local_auth: "true"
komodo_disable_user_registration: "true"
komodo_enable_new_users: "false"
komodo_disable_non_admin_create: "false"
komodo_transparent_mode: "false"
