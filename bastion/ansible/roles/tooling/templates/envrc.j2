# Managed by Ansible - do not edit manually
source .venv/bin/activate

# Terraform - Proxmox provider
export TF_VAR_pm_api_url="https://proxmox.local/api2/json"
export TF_VAR_pm_api_token_id=$(pass proxmox/api-token-id)
export TF_VAR_pm_api_token_secret=$(pass proxmox/api-token-secret)

# Terraform - GitHub provider
export TF_VAR_github_owner="TiPunchLabs"
export TF_VAR_github_token=$(pass github/terraform-token)

# Ansible
export ANSIBLE_VAULT_PASSWORD=$(pass ansible/vault)
