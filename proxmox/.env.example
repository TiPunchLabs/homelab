# =============================================================================
# Proxmox Ansible Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control!

# -----------------------------------------------------------------------------
# SSH Configuration
# -----------------------------------------------------------------------------
# SSH user for connecting to VMs (default: ansible)
SSH_USER=ansible

# Path to SSH public key file (default: ~/.ssh/proxmox.pub)
SSH_KEY_FILE=~/.ssh/proxmox.pub

# -----------------------------------------------------------------------------
# Proxmox Server Configuration
# -----------------------------------------------------------------------------
# Proxmox server IP address
PROXMOX_IP=YOUR_PROXMOX_IP

# Password for Proxmox users/tokens generation
# Used by setup_roles_users_tokens.sh script
# IMPORTANT: Use a strong password and store securely in Ansible Vault
GEN_PASS=your_secure_password_here

# -----------------------------------------------------------------------------
# Cloud-Init Configuration
# -----------------------------------------------------------------------------
# Root password for Cloud-Init VM templates
# IMPORTANT: Use a strong password, preferably from vault
CLOUD_INIT_ROOT_PASSWD=your_secure_root_password_here

# -----------------------------------------------------------------------------
# Ansible Vault
# -----------------------------------------------------------------------------
# Path to vault password file (configured in ansible.cfg)
# Managed via pass + direnv (see .envrc at project root)
# VAULT_PASSWORD_FILE is set automatically by scripts/ansible-vault-pass.sh

# -----------------------------------------------------------------------------
# Additional Notes
# -----------------------------------------------------------------------------
# 1. After copying this file to .env, run: chmod 600 .env
# 2. Add .env to .gitignore to prevent accidental commits
# 3. Store sensitive values in Ansible Vault when possible
# 4. Use strong, unique passwords for all services
